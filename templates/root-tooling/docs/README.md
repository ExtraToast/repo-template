# Docs and ADR Presets

Choose one ADR layout:

- `adr/flat/` for one ADR sequence across the repository.
- `adr/domain/` for separate API/frontend/platform ADR sequences.

Copy `indexes/docs-index.md.tmpl` to `docs/README.md` when a repository needs a
simple documentation landing page.
